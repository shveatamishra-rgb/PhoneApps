<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws all UI colors; this window theme just covers pre-Compose paint.
         DeviceDefault.DayNight (API 29+) follows the system light/dark setting. -->
    <style name="Theme.GalleryTransfer" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/background_dark</item>
    </style>
</resources>
