<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Optional: city-level location for accurate Panchang sunrise. Coarse is enough.
         Used only on-device; never transmitted, geocoded, or stored. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Optional: daily darshan reminder (runtime-requested on API 33+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Voice Japa: on-device onset counting only; audio is never recorded or transmitted. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required by Google Play Billing and the in-app review API. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BhaktiAnganApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BhaktiAngan">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.BhaktiAngan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Reschedules the daily reminder after device reboot. -->
        <receiver
            android:name=".core.notify.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".core.notify.ReminderReceiver"
            android:exported="false" />

        <!-- Home-screen widgets (Glance): Daily Shlok, Choghadiya, Daily Darshan. -->
        <receiver android:name=".core.widget.ShlokWidgetReceiver" android:exported="false">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_shlok_info" />
        </receiver>
        <receiver android:name=".core.widget.ChoghadiyaWidgetReceiver" android:exported="false">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_choghadiya_info" />
        </receiver>
        <receiver android:name=".core.widget.DarshanWidgetReceiver" android:exported="false">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_darshan_info" />
        </receiver>
    </application>
</manifest>
